def rotate_array(arr, k):
    n = len(arr)
    k = k % n

    rotated = arr[-k:] + arr[:-k]

    return rotated

my_array = [1, 2, 3, 4, 5]
k = 2
rotated_array = rotate_array(my_array, k)
print(rotated_array)
